<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
    <title>Create new <%= tableName %></title>
</head>
<body>
    <div class="d-flex flex-column align-items-center vh-100 justify-content-start" style="padding-top: 50px;">
        <h1 class="text-center mb-4">Create New <%= tableName %></h1>
        <div class="card p-4 mb-4" style="width: 400px;">
            <form action="/admin/<%= tableName %>/addNew" method="POST">
                <% colNames.forEach (col => { %>
                    <div class="form-group">
                        <label for="<%= col %>"><%= col %></label>
                        <input id="<%= col %>" class="form-control" name="<%= col %>" placeholder="<%= col %>" type="text" />
                    </div>
                <% }) %>
                <button type="submit" class="btn btn-primary mt-2">Add <%= tableName %></button>
            </form>
        </div>
    </div>
    <footer class="bg-light text-center py-3 mt-auto">
        <a href="/admin" class="mx-2 text-decoration-none">Back to Dashboard</a>
    </footer>
</body>
</html>